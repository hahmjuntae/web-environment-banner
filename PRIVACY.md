# 개인정보 처리방침 — Web Environment Banner

최종 수정일: 2026-07-30

## 요약

Web Environment Banner는 **어떤 데이터도 수집·전송·판매하지 않습니다.** 외부 서버와 통신하지 않으며,
분석 도구나 광고 SDK를 포함하지 않습니다. 확장의 모든 코드는 설치 패키지 안에 포함되어 있고,
원격에서 코드를 불러오지 않습니다.

## 저장하는 정보

확장이 저장하는 것은 **사용자가 설정 화면에서 직접 입력한 값**뿐입니다.

| 항목 | 예시 | 저장 위치 |
|---|---|---|
| 환경 이름 | `LOCAL`, `PRODUCTION` | `chrome.storage.sync` |
| URL 패턴 | `localhost:3000`, `*.dev.example.com` | `chrome.storage.sync` |
| 색상·배너 스타일 | `#ec4860`, 높이 26px | `chrome.storage.sync` |
| 배너 숨김 여부 | 탭 세션 한정 플래그 | 해당 사이트의 `sessionStorage` |

`chrome.storage.sync`는 크롬이 제공하는 저장소로, 사용자가 크롬에 로그인한 경우 **구글 계정을 통해
본인의 다른 크롬에만** 동기화됩니다. 이 동기화는 크롬의 기능이며, 개발자는 해당 데이터에 접근할 수 없습니다.

## 방문 기록에 대해

확장은 페이지 주소를 **기기 안에서만** 확인합니다. 현재 주소가 사용자가 등록한 패턴과 일치하는지
비교하는 것이 유일한 용도이며, 그 결과로 배너를 표시할지 결정합니다.

- 방문 기록을 저장하지 않습니다.
- 방문 기록을 외부로 보내지 않습니다.
- 페이지의 내용(텍스트·입력값·쿠키·토큰)을 읽지 않습니다.

## 권한을 사용하는 이유

| 권한 | 이유 |
|---|---|
| `storage` | 위 표의 설정값을 저장하기 위해 |
| `activeTab` | 툴바 팝업을 열었을 때, 현재 탭 주소를 보여주고 원클릭으로 등록하기 위해 |
| 모든 사이트에서 실행 (`<all_urls>`) | 사용자가 **어떤 주소든** 환경으로 등록할 수 있어야 하므로, 배너를 그릴 대상 사이트를 미리 특정할 수 없습니다. 실제로 배너가 표시되는 것은 사용자가 등록한 패턴과 일치하는 페이지뿐입니다. |

## 제3자 제공

없습니다. 데이터가 기기를 떠나지 않습니다.

## 포함된 오픈소스

- Pretendard (SIL Open Font License 1.1) — 확장 패키지에 폰트 파일로 포함. 외부 CDN을 사용하지 않습니다.

## 문의

이 방침에 대한 문의는 크롬 웹스토어 등록 페이지의 지원 연락처를 이용해 주세요.
